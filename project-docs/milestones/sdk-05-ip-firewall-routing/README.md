# SDK-05 — IP, Firewall & Routing

Typed APIs:

```ts
client.ip.address.list();
client.ip.route.list();
client.ip.dns.settings();
client.firewall.filter.list();
client.firewall.nat.list();
client.firewall.addressList.list();
```
