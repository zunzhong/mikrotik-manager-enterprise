# ENTERPRISE-DEVICE-01 — Router CRUD + SDK Probe

API:

```txt
GET  /api/v1/routeros/health
POST /api/v1/routeros/probe
POST /api/v1/routeros/identity
POST /api/v1/routeros/resource
POST /api/v1/routeros/routerboard
```
