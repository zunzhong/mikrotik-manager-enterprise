# BUILD-FIX-01 — Clean Typecheck Patch

Fix group:

- server dependency to `@mme/routeros-sdk`
- safe RouterOS route registration
- remove snippet compile error
- normalize RouterOS probe input
- add web `apiPut`
- export `Device` type
