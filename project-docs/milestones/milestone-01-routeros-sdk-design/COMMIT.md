# Commit

Copy this folder to:

```txt
project-docs/milestone-01-routeros-sdk-design/
```

Then commit:

```powershell
git add .
git commit -m "docs(routeros): add sdk architecture milestone"
git push
```
