# Commit

Copy this folder to:

```txt
project-docs/milestones/milestone-01-routeros-sdk-design/phase-a-sdk-specification/
```

Then commit:

```powershell
git add .
git commit -m "docs(routeros): add sdk implementation specification"
git push
```
