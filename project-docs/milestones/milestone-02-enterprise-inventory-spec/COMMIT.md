# Commit

Copy this folder to:

```txt
project-docs/milestones/milestone-02-enterprise-inventory-spec/
```

Then commit:

```powershell
git add .
git commit -m "docs(inventory): add enterprise inventory specification"
git push
```
