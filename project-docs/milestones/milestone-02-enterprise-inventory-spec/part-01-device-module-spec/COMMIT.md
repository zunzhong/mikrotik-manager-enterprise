# Commit

Copy this folder to:

```txt
project-docs/milestones/milestone-02-enterprise-inventory-spec/part-01-device-module-spec/
```

Then commit:

```powershell
git add .
git commit -m "docs(device): add device module specification"
git push
```
