# ENTERPRISE-DEVICE-06 — Interface Explorer

Adds React interface explorer based on inventory snapshots.

Usage:

```tsx
import { DeviceInterfaceExplorerPage } from './modules/devices';

<DeviceInterfaceExplorerPage deviceId="DEVICE_ID" />
```

Optional CSS:

```ts
import './modules/devices/device-interface-explorer.css';
```
