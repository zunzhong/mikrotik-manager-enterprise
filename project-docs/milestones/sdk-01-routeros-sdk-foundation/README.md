# SDK-01 — RouterOS SDK Foundation

This SDK follows MikroTik RouterOS API protocol: variable-length encoded words, sentences terminated by zero-length words, and replies such as `!re`, `!done`, `!trap`, `!fatal`.

New package: `packages/routeros-sdk`.
