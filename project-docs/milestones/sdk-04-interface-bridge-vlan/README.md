# SDK-04 — Interface, Bridge & VLAN

Typed APIs added:

```ts
client.ethernet.list();
client.bridge.list();
client.bridge.port.list();
client.vlan.list();
client.wireless.list();
client.wifi.list();
```
