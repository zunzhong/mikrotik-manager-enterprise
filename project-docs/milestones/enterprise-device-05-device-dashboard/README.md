# ENTERPRISE-DEVICE-05 — Device Dashboard

Adds React device dashboard foundation.

Import example:

```tsx
import { DeviceDashboardPage } from './modules/devices';

<DeviceDashboardPage deviceId="DEVICE_ID" />;
```

Optional CSS:

```ts
import './modules/devices/device-dashboard.css';
```
