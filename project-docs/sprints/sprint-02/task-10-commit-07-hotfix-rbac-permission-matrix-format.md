# Hotfix — RBAC Permission Matrix Format

Fixes `pnpm format:check` warning for:

```txt
project-docs/security/rbac-permission-matrix.md
```

## Check

```powershell
pnpm format:check
pnpm --filter @mme/server typecheck
pnpm --filter @mme/web typecheck
pnpm build
```
