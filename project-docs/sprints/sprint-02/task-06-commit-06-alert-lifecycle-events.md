# Sprint 02 Task 06 Commit 06 — Alert Lifecycle Events

Publishes Event Bus events when alerts are opened, acknowledged, or resolved.

## Check

```powershell
pnpm --filter @mme/server typecheck
pnpm --filter @mme/web typecheck
pnpm build
```
