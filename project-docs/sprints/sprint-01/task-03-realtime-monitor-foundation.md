# Sprint 01 Task 03 — Realtime Monitor Foundation

Adds UI foundation for realtime device monitoring.

Current source is latest DB inventory snapshot with auto refresh. Backend WebSocket/SSE streaming will be added in a later task.

## Check

```powershell
pnpm --filter @mme/web typecheck
pnpm build
```
